# # Server Configuration
# server.port=8080

# # Database Configuration
# spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/msapay?useSSL=false&allowPublicKeyRetrieval=true}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:msapay}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:msapay}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # JPA Configuration
# spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=${SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT:org.hibernate.dialect.MySQL8Dialect}
# spring.jpa.properties.hibernate.format_sql=true

# # Kafka Configuration
# spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
# spring.kafka.consumer.group-id=payment-service-group
# spring.kafka.consumer.auto-offset-reset=earliest

# # Logging Configuration
# logging.level.com.msapay.payment=DEBUG
# logging.level.org.springframework.data.jpa=DEBUG
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# # Actuator Configuration
# management.endpoints.web.exposure.include=health,info,metrics
# management.endpoint.health.show-details=always
